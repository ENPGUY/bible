const $=id=>document.getElementById(id); function status(m){$("status").textContent=m} async function go(mode){if(!db){status("먼저 js/config.js의 Supabase 설정을 입력하세요.");return} const email=$("email").value.trim(),password=$("password").value; if(!email||password.length<6){status("이메일과 6자 이상의 비밀번호를 입력하세요.");return} const result=mode==="signup"?await db.auth.signUp({email,password}):await db.auth.signInWithPassword({email,password}); if(result.error){status(result.error.message);return} if(mode==="signup"&&!result.data.session){status("가입 확인 이메일을 확인한 뒤 로그인하세요.");return} location.href="./"} $("loginBtn").onclick=()=>go("login"); $("signupBtn").onclick=()=>go("signup");